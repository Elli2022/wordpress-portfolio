# Copy to .env.local in the repo root (Next.js reads this at build/dev time).
# Must be the full GraphQL endpoint URL — same as the portfolio app expects.
wordpressApiKey=http://localhost:8081/graphql
NEXT_PUBLIC_DEPLOY_URL=https://your-deployed-site.vercel.app
